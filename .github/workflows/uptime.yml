#
# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the Upptime template updates (by default, weekly)
# Instead, change .upptimerc.yml configuration and the workflows will be generated accordingly.
#
# 🔼 Upptime @v1.43.9
# GitHub-powered open-source uptime monitor and status page by Anand Chowdhary
#
# LOCAL EDIT (2026-09-02): `SECRETS_CONTEXT: ${{ toJson(secrets) }}` removed.
# That expression serialises every repo secret into an env var, and GitHub now
# gates runs that use it — this workflow sat at conclusion `action_required`, 0s,
# never executing, from 2026-08-30 14:00 until it was removed. Nothing consumed it:
# .upptimerc.yml has no $SECRET interpolation and no notifications: block, and
# GH_PAT is passed separately. If setup.yml ever regenerates this file it will
# reinstate the line and silently stop uptime monitoring again.

# * Source: https://github.com/upptime/upptime
# * Docs and more: https://upptime.js.org
# * More by Anand Chowdhary: https://anandchowdhary.com


name: Uptime CI
on:
  schedule:
    - cron: "*/5 * * * *"
  repository_dispatch:
    types: [uptime]
  workflow_dispatch:
concurrency:
  group: ${{ github.repository }}-${{ github.head_ref || github.ref_name }}-upptime
  cancel-in-progress: false
jobs:
  release:
    name: Check status
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          token: ${{ secrets.GH_PAT || github.token }}
      - name: Check endpoint status
        uses: upptime/uptime-monitor@v1.43.9
        with:
          command: "update"
        env:
          GH_PAT: ${{ secrets.GH_PAT || github.token }}
